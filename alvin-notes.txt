built a Streamlit dashboard for the job postings data, following the same structure as your resale flat example (sidebar filters → KPI row → charts → expandable raw data with download).

What it does:

Filters: Category, Employment Type, Position Level, Average Salary range, Posting Date range
KPIs: Job postings count, average/median salary, total vacancies
Charts: Average salary by category (top 10), postings by employment type, monthly median salary trend, top 10 hiring companies
Expander: full filtered table + CSV download


90.6MB main file + 1.7MB lookup — comfortably under 100MB, no compression needed. Here's what I did:

Dropped title — free text, not used for any filter or chart, and the single biggest column (45MB of the 158MB).
Removed 30,363 exact-duplicate rows — likely re-scraped or reposted listings that repeated the same 22 fields verbatim.
Normalized company into a separate lookup table — 53,151 unique names were being repeated across ~1M rows (~37MB of text). Now the main file just stores a small company_id, and companies.csv maps id → name.
Cleaned average_salary from 2400.0 (float) to 2400 (int) — it was always a whole number anyway.

Now let me update app.py to join the company lookup back in for the "Top Hiring Companies" chart, and refresh the deliverable.

Done — total footprint is ~89MB (87MB main file + 1.8MB lookup), under the 100MB GitHub limit as plain CSVs, no gzip needed.

What got cut, in order of impact:

Dropped title (free text, ~45MB, unused by any filter/chart)
Normalized company into companies.csv — 53k names were repeated across ~1M rows; now it's a compact company_id joined back in on load
Removed 30,363 exact-duplicate rows (reposted/re-scraped listings)
Rounded average_salary from float to int

app.py now loads both files and joins the company name back in, so the rest of the dashboard logic is unchanged. Just remember: if your repo still has the old 150MB CSV committed anywhere in history, you'll need to clean that out (filter-repo or a fresh git init) before this smaller version will push cleanly.

how can i refine this app.py file to give me more relevent fields pertaining to answering the question on hard to fill vacancies in the job industry?

Ran a command
Good — repost_count is a strong, direct signal (an employer reposting a job means they couldn't fill it the first time). Let's check posting-duration fields too:

Ran 3 commands
category, employment_type, and position_level are text values repeated ~1M times each (only 43/8/9 unique values) — the same waste pattern as company. Normalizing those the same way should easily get us under 100MB even with the new hard-to-fill fields added.

Ran a command
Excellent — 63MB main file + 1.8MB companies + 2KB lookups = ~65MB total, well under budget with room to spare. Now let's rebuild app.py around this schema and around answering the hard-to-fill question directly.

Created 2 files, ran 2 commands
Everything works end to end. Overall hard-to-fill rate is 75% at the default threshold — makes sense, since most SME job ads get few applications. Let's verify the file compiles cleanly, then package it.

Ran 2 commands, edited a file, shared files
Here's the refined version, now built specifically to answer "which vacancies are hard to fill."

New fields added (pulled back in from columns the earlier cleanup had dropped):

repost_count — direct signal: employer reposted because the first listing didn't fill
posting_duration_days — how long the listing ran
applications_per_vacancy — applicant interest normalized against how many roles need filling
views_per_application — people who viewed but didn't apply (interest without conversion)
A posting is flagged "hard to fill" if it was reposted, or its applications-per-vacancy falls below an adjustable sidebar threshold (default 1.0). New sections in the app:

Hard-to-fill rate by category and by position level — quick sanity check: Personal Care/Beauty, Medical/Therapy Services, and entry-level roles come out worst in the full dataset
A salary vs. applications scatter — separates "genuinely hard to fill despite good pay" from "just underpaid"
Monthly trend of hard-to-fill rate
Top companies by hard-to-fill posting count (heads up: several of the top names are recruitment agencies posting on behalf of many employers, worth keeping in mind when reading that chart)
Bonus: normalizing category/employment_type/position_level/job_status into a tiny lookups.csv (same trick as the company table) shrank the main file from ~104MB down to ~61MB, so there's plenty of headroom left in the 100MB budget even with the new columns.
